# appium_device_lifecycle
Associates Appium node instances lifecycles with a device connection.

## Build profiles

### Appium Node Manager
mvn clean package -Pappium-nodes

### Selenium Grid Capability Matcher
mvn clean package -Pselenium-grid
